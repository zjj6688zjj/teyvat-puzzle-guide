# 原神AI攻略助手（MVP版） · 使用说明

一个小工具：把你自己攒的攻略喂给它，然后直接问它就行。比如"这个解密怎么解？""隐藏锚点在哪？"，它会从你的攻略里翻出相关的内容，回答你，并告诉你是从哪篇攻略来的。

## 系统要求：

电脑系统 Windows 10 或 11，装好 Python 3.10 或更高版本（安装时记得勾上 **Add python.exe to PATH**），能正常上网就行。

建议把文件夹解压到一个纯英文路径（比如 `D:\GenshinAI`），省得个别电脑碰到中文路径闹脾气。

> ⚠️ **Python 的 Scripts 目录也要加进 PATH**。装完 Python 后，把它的 `Scripts` 文件夹（一般在 `C:\Users\你的用户名\AppData\Local\Programs\Python\Python310\Scripts`）加到系统环境变量 PATH 里，否则后面敲 `pip`、`streamlit` 这些命令时会报"不是内部或外部命令"。加的方法：设置 → 系统 → 关于 → 高级系统设置 → 环境变量 → 双击 Path → 新建 → 粘贴上面那个 Scripts 路径 → 确定，然后**重新开一个终端**再试。

## 第一步：装依赖

打开 PowerShell，进到解压好的目录，按顺序敲：

```powershell
cd "您解压的文件夹路径"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

看到命令行前面多出个括号 `(GenshinAI)`（就是你解压的文件夹名），说明环境就绪了。

## 第二步：配两个密钥

这个工具要用两个密钥，一个管"写回答"，一个管"理解文字"。都不难，弄一次以后就不用管了。

1. **DeepSeek**（写回答）：打开 platform.deepseek.com 注册 → 充点钱（最低 10 块）→ 在「API Keys」里创建一个，复制那串 `sk-` 开头的密钥
2. **阿里百炼**（理解文字）：阿里云里搜"百炼"→ 在「API-KEY 管理」里创建一个；如果 `qwen3.7-text-embedding` 这个模型提示没权限，先去模型广场开通一下

先复制配置模板：

```powershell
copy .env.example config\.env
```

然后用记事本打开 `config\.env`，把两个密钥填进去：

```
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
DASH_SCOPE_API_KEY=sk-你的百炼密钥
```

填好验证一下通不通：

```powershell
python scripts/check_env.py
```

看到两行 `[OK]`，最后还有"全部通过 ✅"，就说明没问题了。

## 第三步：建知识库

这一步别跳过，不然助手不知道你问的到底是啥。按你的数据情况二选一：

**方式 A：自己有攻略文档（推荐）**

把攻略存成 `.md` 或 `.txt` 文件，丢进 `data\raw\text_guides` 文件夹（这个文件夹已经建好了，直接往里放）。想标注来源的话，在文件最开头加一行：

```
<!-- source_url: https://你攻略的链接 -->
```

**方式 B：有米游社爬虫数据**

把 `output` 文件夹整个放到本项目文件夹的**上一级**（跟本项目文件夹并列），不用动 `data\raw`。目录名如果想改，去 `config\settings.yaml` 里改 `raw_output`。

**不管哪种方式，放好数据后执行：**

```powershell
python scripts\build_kb.py
```

以后再加了新攻略，重新跑一次这条命令就行，它只加新的，不覆盖旧的。

## 第四步：打开用

```powershell
streamlit run src/app/chat.py
```

浏览器会自动打开（没自动开就手动访问 http://localhost:8501）。左边能看到知识库里现在有多少条，下面输入框直接打字提问。回答里带的 [1][2] 是引用标记，对应下方的来源列表，点链接能看到原文。

## 常见问题

**问：打开后显示"知识库还是空的"？**
答：还没建库。按第三步把数据放好（攻略文档放 `data\raw\text_guides`，或米游社数据放项目上一级的 `output`），再跑一次 `python scripts\build_kb.py`。

**问：报"缺少环境变量"？**
答：`config\.env` 没建好，或者两个密钥名字填错了。回第二步检查。

**问：报 401 / 认证失败？**
答：密钥不对，或者 DeepSeek 的余额用完了（它是预付费的）。

**问：它说"知识库中暂无该信息"？**
答：说明这个问题你喂的攻略里没有，很正常。找篇相关攻略加进去，再跑一次建库就行。

**问：回答有点慢（几秒到十几秒）？**
答：模型在思考，界面是边想边打字的，稍等一下就好。

**问：想换模型或者调参数？**
答：改 `config\settings.yaml`，改完重启界面。

**问：重开终端后跑命令报"不是内部或外部命令"？**
答：终端得先回到项目目录，并重新激活虚拟环境（第一步的前两条命令），再跑其他命令。

## 几个提醒

- `config\.env` 里有你的密钥，别发给别人，也别传到网上。
- 目前 MVP 版只实现攻略问答。地图标注、视频跳转那些功能还没有。
- 回答好不好，很大程度看你喂的攻略全不全；纯图片的攻略它读不到文字，可能会漏细节。

## 单元测试（可选）

- 单元测试：`python tests\test_pipeline.py`
- 评测：自己写评测题放进 `data\eval\questions.jsonl`（每行一条 JSON，如 `{"question": "这个解密怎么解？", "expect": "answer"}`），然后跑 `python scripts\eval.py`
